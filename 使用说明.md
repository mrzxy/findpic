# 头像匹配工具 - 使用说明

## 快速开始

### 1️⃣ 安装依赖

```bash
pip install opencv-python opencv-contrib-python numpy
```

### 2️⃣ 运行匹配

```bash
python find_avatar.py photo.png msg.png
```

## 实际案例

### ✅ 成功案例：photo.png 在 msg.png 中

运行：
```bash
python find_avatar.py photo.png msg.png
```

结果：
```
✅ 找到匹配！

策略: 中心区域
置信度: 48.99%

位置信息:
  坐标: (54, 32)
  大小: 30 × 30 像素
  中心: (69, 47)
  缩放: 0.30x

✓ 结果图已保存: avatar_found.png
```

**说明**：成功在聊天截图左上角找到了用户头像（圆形，紫色边框）

## 工作原理

### 智能匹配策略

程序会自动尝试 4 种匹配策略，选择最佳结果：

1. **直接匹配** - 标准模板匹配
2. **圆形遮罩** - 模拟圆形头像裁剪
3. **中心区域** ⭐ - 只匹配头像中心部分（最适合本案例）
4. **边缘检测** - 基于轮廓匹配

### 为什么选择"中心区域"？

- ✅ 避免边缘差异（方形 vs 圆形）
- ✅ 忽略边框和背景
- ✅ 专注于人脸核心特征
- ✅ 对裁剪变化更鲁棒

## 文件说明

### 核心程序

- **find_avatar.py** - 主程序（命令行工具）⭐ 推荐使用
- **smart_avatar_matcher.py** - 智能匹配器（核心算法）
- **image_matcher.py** - SIFT 特征匹配器
- **template_matcher.py** - 模板匹配器

### 测试脚本

- **test_match.py** - 测试 SIFT 匹配
- **test_template.py** - 测试模板匹配

### 示例

- **example.py** - 详细使用示例

## 常见问题

### Q1: 为什么 SIFT 匹配失败？

A: SIFT 适合大图、特征丰富的场景。对于小头像（26个特征点），容易匹配失败。

### Q2: 智能匹配器如何工作？

A: 自动尝试多种策略，选择分数最高的结果。针对头像优化了圆形裁剪处理。

### Q3: 匹配分数多少算成功？

A:
- 直接匹配：> 30%
- 圆形遮罩：> 30%
- 中心区域：> 30%
- 边缘检测：> 25%

### Q4: 如何提高匹配成功率？

1. 使用清晰的头像图片
2. 确保头像和聊天截图来自同一来源
3. 避免过度压缩的图片
4. 尝试使用 `smart_avatar_matcher.py`

## 进阶用法

### 批量处理多张聊天记录

```python
from smart_avatar_matcher import SmartAvatarMatcher

matcher = SmartAvatarMatcher()
chat_files = ["chat1.png", "chat2.png", "chat3.png"]

for chat_file in chat_files:
    result = matcher.find_avatar("photo.png", chat_file)
    if result['found']:
        print(f"✓ {chat_file}: 找到头像 ({result['score']:.2%})")
    else:
        print(f"✗ {chat_file}: 未找到")
```

### 调整匹配阈值

如果误报太多，可以提高阈值：

```python
# 编辑 smart_avatar_matcher.py
# 将 if best_score > 0.3 改为 if best_score > 0.5
```

### 保存所有候选位置

```python
result = matcher.find_avatar("photo.png", "msg.png", visualize=True)

# 保存结果
import cv2
cv2.imwrite('result.png', result['visualization'])
```

## 技术对比

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **智能匹配** | 自适应，准确率高 | 速度稍慢 | 聊天头像（推荐）|
| **SIFT** | 对旋转缩放鲁棒 | 小图效果差 | 大图、特征丰富 |
| **模板匹配** | 速度快 | 对变形敏感 | 大小相近的图片 |

## 许可

MIT License
